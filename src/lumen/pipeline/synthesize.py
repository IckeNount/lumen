"""Structured report synthesis with citations — streaming in week 2."""

from __future__ import annotations


def synthesize_report(
    question: str,
    passages: list[dict[str, str]],
    *,
    stream: bool = False,
) -> str:
    """Produce markdown report with explicit citations — validate schema in week 3."""
    raise NotImplementedError("Week 2: prompt versioning + streaming + citation guards.")
