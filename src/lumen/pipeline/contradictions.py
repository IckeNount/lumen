"""Detect and summarize disagreements across retrieved passages (week 2)."""

from __future__ import annotations


def find_contradictions(passages: list[dict[str, str]]) -> list[dict[str, str]]:
    """Return structured contradiction items with source ids — LLM + checks in week 2."""
    raise NotImplementedError("Week 2: pairwise or set-level contradiction pass.")
