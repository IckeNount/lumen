"""Fetch HTML and extract main text with trafilatura/BS4 (week 2)."""

from __future__ import annotations


def fetch_url(url: str) -> str:
    """Return cleaned plain text for a single URL."""
    raise NotImplementedError("Week 2: httpx fetch + trafilatura with size limits.")
