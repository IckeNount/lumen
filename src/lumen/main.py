"""CLI / process entrypoint — expanded in week 4 with ASGI app wiring."""

from __future__ import annotations


def main() -> None:
    """Placeholder until FastAPI app is mounted from `lumen.api.app`."""
    print("Lumen: use `uvicorn lumen.api.app:app` once the API module is wired.")


if __name__ == "__main__":
    main()
