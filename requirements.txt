# Lumen local runtime and tests (Python 3.11–3.12; see README)

# OpenAI-compatible client for the secondary FastAPI pipeline
openai>=1.40.0,<2

# HTTP client
httpx>=0.27.0,<1

# Configuration & validation
pydantic>=2.8.0,<3
pydantic-settings>=2.4.0,<3
python-dotenv>=1.0.1,<2

# Local vector store and semantic embedding model
chromadb>=0.5.16,<0.6

# Web search API (choose one; both wired in config)
tavily-python>=0.5.0,<1

# Secondary HTTP interface
fastapi>=0.115.0,<1
uvicorn[standard]>=0.30.0,<1

# Local host integration — thin stdio MCP adapter
mcp>=2.0.0,<3

# HTML extraction / cleaning (fetch → text)
trafilatura>=1.6.0,<2

# Tests
pytest>=8.0.0,<9
pytest-asyncio>=0.24.0,<1
